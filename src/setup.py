from setuptools import setup

setup(name='code_query',
      version='0.1',
      description='Internal private package for the code-query project',
      author='Frans Johansson',
      packages=['code_query'])
